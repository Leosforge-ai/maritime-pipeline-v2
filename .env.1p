# 1Password fallback (company standard: .env is primary; use this only when a secret
# is not already in your .env). Run:  op run --env-file=.env.1p -- <command>
# These are op:// references, NOT secret values, so this file is safe to commit.
#
# TODO: replace the placeholder reference with the real vault/item/field path
# (needs 1Password vault access — fill in when wiring the vault).
MOTHERDUCK_TOKEN=op://Private/MotherDuck/credential
